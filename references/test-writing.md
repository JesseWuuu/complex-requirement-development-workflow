# 测试文档写作指南

本参考文件扩展父级 `SKILL.md` 的核心文档规则，不替代或覆盖这些规则。仅在编写或实质修订 `test.md` 前读取。

## 核心审阅问题

围绕已批准 Spec 的每项关键契约，哪些真实业务情况和外部结果构成必要覆盖义务，并能约束后续实施而不扩展产品契约或预设内部方案？

## 成文依据

测试文档紧随 Spec，在选择实施方案之前形成。写入前确认：

- 已批准且仍有效的 Spec 是产品行为的唯一权威，每条关键 `R-*` 都已识别；
- 已读取实现准入场景和最小修改假设所必需的真实 owner、业务分支、数据边界及相关测试基础设施；
- 需求事实、当前代码事实和预期行为已区分；现有代码与 Spec 冲突时，测试以 Spec 为期望，并把冲突标为受影响回归或待改行为；
- 不依赖 `implementation.md`、`I-*` 或尚未批准的未来文件、符号和内部调用顺序；
- 场景只定义业务前置、触发和外部可观察结果，不选择测试层级、内部断言或生产实现机制。

## 场景准入

每个行为场景必须通过主 Skill 阶段 4 的准入，并在工作笔记中标明它来自已批准 `R-*`、必须保留的既有外部行为，还是会产生不同已批准结果的真实业务入口。

请求次数、防御性拷贝、token/generation、同步、缓存内部状态、线程与架构结构等内部性质，除非本身对应已批准的外部契约，否则不生成 `T-*`。它们可以作为 Implementation 约束，并在代码实施阶段选择必要的实现级测试，但不能反向要求 Spec 定义新行为。

写用例前，只在上述准入范围内从可观察结果反向核对初次观察、互斥业务条件、会改变已批准结果的真实生产者或入口，以及已批准的失败或恢复语义。不得为了显得覆盖完整而枚举通用异步、生命周期或账户组合。

## 覆盖边界

测试文档定义按风险排序的行为覆盖义务，是后续实施文档的强制输入，而不是脱离项目逻辑的 QA 清单。它只保留行为测试用例和受影响回归；不要重复 Spec，也不要扩展通用检查清单。

- 行为场景只描述外部可观察事实。
- UI 变化属于 Spec 契约时，只把可观察结果写入对应行为场景，不建立独立 UI 或手工检查清单。
- 只有测试依赖某项准备条件时，才说明对应设置细节。
- 必要的测试准备与当前仓库中真实可执行的测试命令只在本文件中定义一次；不能确认或尚不存在的命令不得猜测。
- 所需验证能力尚不存在时，记录“待实施验证目标”，只说明必须覆盖的集成边界、业务场景和通过信号，不预设未来任务名、文件或测试层级；阶段 5 必须用具体 `I-*` 承接其新增或扩展。
- 资源、Manifest、导航、依赖注入、代码生成、实验或变体接线发生变化时，现有命令或待实施验证目标必须覆盖真实集成边界；不能只用更窄的单元测试或源码编译任务替代。

## 从契约展开情况

先把每条关键 `R-*` 映射到至少一个可观察场景，再只依据证据选择会改变已批准外部结果的变化维度，例如：

- 主路径及互斥的业务条件、权限、实验组、角色或数据来源；
- 已知真实业务生产者或入口导致的后续变化；
- Spec 已定义的空态、失败、恢复或兼容行为；
- 有证据且必须保留的相邻旧功能回归。

只保留通过场景准入且会改变外部结果的组合。合并结果等价的排列，避免把维度做成无差别笛卡尔积。发现 Spec 没有定义某个必要的可观察结果时，将其作为产品阻塞项返回阶段 3，不在测试场景里自行决定。

## 行为测试用例

使用 Markdown 标题和 Given-When-Then 分组列表描述外部可观察行为。场景标题使用比所属覆盖章节低一级的标题；位于“行为测试用例”下时默认使用三级标题。只有场景会被其他位置引用时才添加 `T-*` ID，否则标题只保留场景名称：

```markdown
### T-01 · <场景>

**Given**

- <业务状态或前置条件>

**When**

- <操作或触发条件>

**Then**

- <外部可观察结果>
```

每个条件、动作或结果单独使用一个列表项。从主路径开始，只添加有意义的分支、负向行为、恢复过程、可见状态变化和受影响回归。`When` 必须写可观察或业务可识别的真实触发，例如完成某项操作、返回页面或刷新失败，不能只写“状态变化”或“收到通知”。场景标题或相邻说明应能追溯到相关 `R-*`，但不要复制规则正文。合并等价变体。行为场景中不要出现符号、文件路径、Fake、挂载细节或内部调用顺序。

## 覆盖审阅

- 每条关键 `R-*` 必须至少由一个 `T-*` 行为场景覆盖；只有映射已经从相邻引用显而易见时才省略汇总表。
- 每个 `T-*` 都通过场景准入；没有把实现约束、架构性质或通用风险提升为产品行为。
- 对状态型或动态 `R-*`，只有会产生不同已批准外部结果的真实生产者和入口才形成独立场景；没有用笼统刷新场景掩盖已知业务差异。
- 无法用业务前置、触发和外部可观察结果表达的规则，应返回 Spec 明确契约，而不是增加内部断言或模糊检查。
- 会约束实施职责、生命周期、失败或并发处置的场景必须使用稳定的 `T-*`；`test.md` 不引用 `I-*`，后续实施文档负责逐项说明这些关键 `T-*` 由已有保证或哪些变更满足。
- 每个“待实施验证目标”都能定位到相关 `T-*`、必要集成边界和通过信号；它不包含猜测命令，并将在阶段 5 由明确的 `I-*` 承接。
- 审阅前删除工作流历史、就绪声明、重复的上游描述和低风险排列组合。
