---
name: complex-requirement-development-workflow
description: "将跨界面、状态、生命周期、集成、实验或 Figma 相关的模糊复杂需求，依次转化为精简且可执行的 Spec、测试与实施文档，并仅在确认后实现代码；支持跨对话续接，以及把必须依次落地的多个模块工作流串行归入同一需求工作区。适用于需要产品决策和仓库依据的需求；不适用于孤立修复、纯 UI 直接实现或已经可以直接实施的计划。"
---

# 复杂需求研发工作流

将复杂需求按 `Spec → Test → Implementation` 转化为三份精简且可执行的文档，并在单独确认后完成代码实现。实施与批准验证完成后，立即由一个未参与当前工作流的全新 Sub Agent 对最终代码做一次只读审阅；审阅只汇报发现、不自动修复，返回结论即结束该模块的七阶段流程。测试先把产品契约转化为结合现有项目逻辑的覆盖义务和实施输入，实施方案再逐项承接契约与测试。所有决策必须基于需求、用户已确认的选择、提供的设计、仓库证据和项目规则。可执行意味着决策完整，而不是证据材料必须全部写入交付文档。

规划是一次停止在首次产品代码写入前的只读实现演练，不是绿地架构设计。先沿当前执行路径确认最小行为差异和最窄可信修改点，再形成文档；实施方案应尽量预测实际会出现的最小 Diff，不为概念整洁、结构对称、未来复用或文档完整度改造现有系统。

编写文档时，以本 `SKILL.md` 为核心规则。本 Skill 本地 `references/` 目录中的文件仅作为条件扩展：必须通过下方声明的路由读取，并且只能在各自范围内应用。如果本地参考文件与本文件冲突，以核心规则为准。

## 首次使用说明

在当前对话中第一次因自动匹配而启用本 Skill，或用户在当前轮通过 `$complex-requirement-development-workflow`、Skill 链接或明确指令主动调用时，先用一段简短说明介绍如何使用，再继续需求归档、阶段判断或恢复工作。正常续接已在运行的工作流且用户未重新主动调用时，不重复介绍。

说明应让用户立即知道：

- 本 Skill 适合需要产品决策与仓库依据的复杂需求；孤立修复、纯 UI 直接实现或已经可以直接实施的计划会转交更轻量的实施流程。
- 工作流按七个阶段推进，核心交付物是 `spec.md`、`test.md` 和 `implementation.md`；每份文档单独确认，阶段 6 独立审阅通过并再次获得明确授权后才修改产品代码，阶段 7 结束时还会做一次只读代码审阅且不自动修复发现。
- 用户只需提供原始需求文本或 PRD，并可补充 Figma、相关任务或期望交付目录；预计跨对话时应说明这一点，多模块需求还应尽量给出模块列表与依赖顺序。Skill 会结合项目规则与仓库现状逐步收敛，不要求用户预先整理成 Spec。
- 跨对话工作会保存精简的本地检查点；用户在新对话中提供原任务、需求目录或检查点路径即可请求恢复。检查点只保留当前阶段、产物批准与摘要、技术依据、实施结果和有效审阅结论，不保存 Agent 调度历史；发生相关漂移时从最早受影响阶段继续。
- 必须依次落地的多模块需求会进入同一需求工作区：共享一份原始 PRD，每个模块分别完成自己的七阶段、三份文档与批准；任何时刻只允许一个模块处于 `in_progress`，前序模块完成实现、验证和终结代码审阅后才能激活下一个模块，不把这些模块作为并行实施包。
- 用户可用“确认方向”“确认 Spec”“确认测试文档”“确认实施文档”或“按 implementation.md 开始实施”等明确表述推进对应节点；“继续”“接着做”和进度询问只恢复或查询状态，不代表批准。

介绍只用于建立使用预期，不新增确认节点，也不要求用户先回答“是否使用本 Skill”。输入已经充分时，介绍后立即执行当前可安全推进的步骤；请求明显不适用时，只需简要说明适用边界并切换到合适流程。

## 文档写作基线

交付文档只保留已经确认、归当前文档负责且审阅或执行必需的决策；完整证据留在可追溯的工作笔记。按当前阶段写作指南的核心问题组织内容，无法快速浏览时才用一个短段落或最多五个要点定位。

写完后删除重复的上游描述、路径/符号、验收条件、命令、过程历史、废弃方案、通用建议和空章节。审阅状态与下一步留在对话中；只有便于比较时才使用表格，不在单元格中堆放长段落。

## 原始需求归档

首次接收原始需求时，在进入阶段 1 前把原始 PRD 归档为交付目录中的单一权威输入。用户提供本地文件时，允许先做一次只读 intake，只读取足以确定需求名、`slug`、单模块/工作区模式、文件格式和目标冲突的最小内容；这不是阶段 1 的产品分析，也不得提前形成方案。随后按“输出位置”确定并说明目标目录、源路径与目标路径：

- 用户直接在对话中提供需求文本时，将原文保真落盘为交付目录中的独立 `prd.md`；不要把 Agent 分析、方案、确认结果或后续补充推断混入原始 PRD。
- 用户提供本地 PRD 文件时，保留原文件名和格式。仅当本次请求已经明确授权移动或用户在看到源路径与目标路径后确认时，才将文件移动到交付目录且不在原位置保留副本；否则在移动前暂停。文件已经位于目标目录时直接复用。目标存在同名文件时，先核对是否为同一内容；不得覆盖不同内容或未经授权改名。
- 一个大型 PRD 覆盖同一需求族的多个模块时，只在需求工作区交付目录根部保存这一份原始 PRD。不得按模块拆分、摘录或复制子 PRD；各模块检查点引用同一路径和摘要，模块范围在各自方向与正式文档中界定。

完成归档后再完整读取 PRD、记录路径与内容摘要并开始阶段 1。归档只改变输入的存放位置，不代表批准其中的方案，也不授权覆盖现有交付物。

## 适用范围与工作模式

如果文档已经存在，定位最早出现实质错误或不完整的阶段，并从该阶段继续。不要重复已经批准的工作。用户要求更新明确指定的现有目标时，只授权修改这些目标；如果修改会使其他未授权文档失效，应指出依赖关系并在编辑它们前取得授权，不能把尚未同步的文档声明为一致。

如果用户说明产品方向已经确定，应将其正式化，不要重新讨论替代方向；但仍需指出矛盾、不成比例的复杂度、安全风险或可行性风险。对于已经可以直接实施的需求或纯 UI 直接实现，不要使用本工作流，应改用对应的实施工作流。

阶段 2 核对后必须重新判断适用性。若当前基线已经满足全部已确认要求且必要 Diff 为零，则给出最小证据并停止，不为既有实现逆向补方案；剩余可观察歧义仍按开放决策处理。若需求已经收敛为单一 owner 内的局部变更，不存在待确认产品行为、外部契约、跨生命周期共享状态或不成比例风险，则停止创建后续三份正式文档，说明其已经可以直接实施，并单独请求直接实施授权；方向批准不构成产品代码授权。不得为了固定产物扩大需求。用户明确要求追溯文档或仍要文档时，按已经证明的最小范围成文。

同一大需求的模块必须依次落地，或用户要求把后续模块加入既有需求目录时，使用严格串行的需求工作区。模块共享需求族级原始 PRD，但各自执行完整七阶段并维护独立文档与批准；工作区只编排顺序、活动 owner 与进度，不保存代码谱系或跨模块内容摘要。后续模块可以登记为 `queued`，但所有前序成员均为 `complete` 且当前任务已经原子取得唯一活动槽前，不得开始模块工作流；同一工作区最多一个 `in_progress` 模块。

## 跨对话续接

工作预计跨对话、正在从本地状态恢复，或上下文丢失已成为风险时，启用 `<spec-root>/.workflow/<feature-slug>/state.yaml`。检查点只保存当前阶段、下一步、阻塞项、三份文档的批准与摘要、技术依据指纹、唯一规划指纹、实施授权、实施结果和两次强制审阅的有效结论；正式决策仍归三份文档。方向只在 Spec 批准前保存精简摘要，Spec 批准后由 Spec 取代，不创建或回填独立方向文件。恢复脚本只校验路径、摘要、指纹和结果绑定；无漂移时只加载当前阶段输入与可选 `grounding-pack.md`，有漂移时只复核脚本指出的目标。Sub Agent 任务、替代关系、配额和进度均不进入检查点。`继续`、`接着做`或旧任务引用只表示恢复，不构成新批准；旧任务中能直接核实且仍绑定当前内容的明确批准可以迁移，含糊或已漂移的批准不迁移。工作区调度继续由 `workspace.yaml` 独立负责。

## 职责与 Sub Agent

主 Agent 负责产品语义、用户沟通、三份文档、确认节点、共享边界和最终整合。阶段 2、4、5、7 的调查或实施只有独立技术面存在实质收益时才派发；产品方向、确认、文档主笔、跨文档修订、强串行链路、重叠写入和局部小修不得委派。阶段 6 不适用收益门槛，必须由未参与文档主笔或关键技术结论的独立审阅者执行只读审阅；无法建立独立审阅时不得通过。阶段 7 的实施与验证完成后另有一次强制只读代码审阅，同样不适用收益门槛；必须使用未参与本工作流规划、实施或审阅的全新 Sub Agent，且不得复用阶段 6 审阅者或阶段 7 实施者。检查点只持久化绑定当前指纹的完整审阅结论；运行中任务在新对话不可恢复、失败或没有完整结论时，对相同输入重新执行一次完整审阅并把尝试次数加一，不保存替代关系。相同输入第二次仍无完整结论时模块阻塞。派发、复审、运行时映射和阻塞规则由委派参考负责。

需求工作区的成员模块不计入 Sub Agent 配额，也不得作为并行实施包运行。隔离任务只是承载某一个已激活模块的执行环境；开始或恢复前必须指向同一工作区，并通过工作区门禁取得该模块的唯一活动槽。

工作笔记仅在证据量、上下文、交接或合理并行确有需要时创建，并放在交付目录之外；它们不是用户交付物。

## 授权与确认节点

仅在以下情况暂停：

1. 阶段 1 的方案方向已经可以确认；
2. 某个产品选择会改变可观察行为、范围、外部契约、不可逆数据、安全、隐私或合规义务；
3. 缺少的证据导致无法形成可执行结论；
4. 已生成的 Spec、测试文档或实施文档可以审阅；
5. 移动用户提供的本地原始 PRD，或覆盖现有目标尚未获得授权；
6. 阶段 6 的有效发现需要修改任一正式文档，已核实发现并形成建议处置，需要用户决定是否以及如何返修；
7. 阶段 6 已通过，需要确认是否开始产品代码实现。

直接应用已经验证的项目命名、架构和文件落点约定，不要把它们变成产品阻塞项。只有在某项不确定性即使判断错误也不会改变产品语义时，才能将其作为有明确范围的非阻塞假设保留。

批准按以下方式向后续阶段传递：

- 方向批准后，可以进行阶段 2 的依据核对，并在阻塞项解决后创建新的 `spec.md`；
- Spec 批准后，可以结合相关项目逻辑创建新的 `test.md`；
- 测试文档批准后，可以根据已批准的 Spec 与测试方案创建新的 `implementation.md`；
- 实施文档批准后，只授权进入阶段 6 的只读审阅，不授权因审阅发现修改任何正式文档；阶段 6 的返修必须先取得用户对返回阶段、受影响文档和修订方向的明确决定，实质修订还必须重新打开所属文档的审阅确认节点；
- 上述任何批准都不授权修改产品代码。

跨对话传递批准时，必须同时满足：记录由明确的用户确认产生、记录绑定的产物路径仍是当前目标、记录的摘要与当前文件一致。三份文档与技术依据聚合成唯一规划指纹；阶段 6 结果、阶段 7 产品代码实施授权和实施结果分别绑定该指纹。任一输入发生实质变化时相关结论与授权失效。

同一任务中只有一个明确待确认节点时，用户直接回复“确认”或“通过”可以批准该节点；阶段 6 返修授权除外，必须明确同意建议返回阶段和受影响文档。存在多个决策、多个产物或跨对话恢复时，批准必须明确指出选择、阶段或产物；`继续`、进度询问和“处理一下”等含糊指令都不能解释为批准。阶段 6 因同一项已确认技术修正同时重开 `test.md` 与 `implementation.md`、且 Spec 未变化时，可以在取得返修授权后按上游到下游顺序修改两份目标，并请求一次明确命名两份产物的联合批准；联合批准不授权产品代码实施。

写入前先说明已经确定的目标路径。如果前一个确认节点已经授权创建新目标，不要因为提纲或第二次写入许可而暂停。覆盖未经用户授权的现有目标前必须询问。

## 阶段

| 阶段 | 结果 | 确认节点 |
| --- | --- | --- |
| 1 | 问题界定与方案方向 | 确认方向 |
| 2 | 只读实现演练、契约分类与最小修改假设 | 只解决阻塞性的产品决策 |
| 3 | 生成 Spec | 审阅 Spec |
| 4 | 生成测试文档 | 审阅测试方案 |
| 5 | 生成实施文档 | 审阅实施文档 |
| 6 | 独立的必要性、跨文档一致性与可读性审阅 | 有返修时先确认处置；通过后确认是否实施 |
| 7 | 产品代码实现、验证与终结只读代码审阅 | 仅在明确确认后进入 |

到达实际确认节点时，以 `阶段 N/7 · <名称> · <状态>` 开头，只呈现结论、需要确认的决策和下一步对话操作。快速修订或常规进度更新时，不要重复完整状态栏。

## 来源权威与追溯

- 需求描述负责说明已报告的问题、参与者、约束和期望行为；其中提出的方案并不自动视为最优方案。
- 用户已确认的决策决定产品语义与方向。
- Figma 只决定其明确呈现的视觉和交互事实。
- 仓库代码和项目规则决定当前行为与实施约束。
- 发现矛盾时必须提出，不要静默处理。

### 契约晋升门槛

阶段 2 的发现必须先分类为：产品契约、必须保留的现有外部行为、实施约束或开放决策。只有前两类可以直接进入 Spec 并产生行为测试；实施约束留在技术依据、工作笔记或 Implementation，开放决策只有经用户确认后才能晋升为产品契约。

- PRD、用户已确认的决策与 Figma 明确呈现的事实可以定义期望产品行为；仓库事实只能证明当前行为、既有外部保证和实施约束，不能单独定义新的期望行为。
- 非上游明示的可观察行为，只有在它是目标成立时唯一且不可避免的结果，或是本次变更必须保留的既有外部行为时，才能进入 Spec。存在两种都满足目标但结果不同的选择时，它是开放决策。
- 并发、迟到响应、重复触发、缓存、线程、生命周期和账户隔离默认是实施约束；只有其外部结果已获批准或属于必须保留的行为时，才晋升为产品契约。

产品规则只使用一个 `R-*` 命名空间，并且仅用于下游文档必须引用的关键规则或契约。`I-*` 仅用于主要实施步骤，`T-*` 仅用于会被其他位置引用的测试。决策和开放问题使用描述性文字。需要下游追溯的已确认决策应并入其所属的 `R-*` 规则；未解决问题使用简短的局部标签，不要再建立一套贯穿文档的 ID 命名空间。不要为同一规则创建成对 ID。

局部修订已经批准的现有文档时，除非用户授权协同重写，否则保留既有 ID；不要仅为符合当前约定而批量重新编号。

Spec 只负责通过契约晋升门槛的产品规则。测试文档只把已批准契约和有证据的既有外部回归转化为行为场景；内部风险不自动产生 `T-*`。所需验证能力尚不存在时，只定义真实集成边界和通过信号，不猜测未来命令、路径或内部结构。Implementation 预测满足已批准 Spec 与测试所需的最小真实 Diff；每个关键 `T-*` 必须由真实 owner 的既有保证或具体变更承接。先更新负责该内容的上游文档，下游只引用关联，不重新定义。

## 按需读取参考文件

- 用户要求创建、加入、恢复或扩展需求工作区，或者当前需求明确由必须串行落地的多个模块组成时，在确定目录、读取前序模块或创建后续模块目标前完整读取 [references/requirement-workspace.md](references/requirement-workspace.md)，并以 [assets/workspace.yaml](assets/workspace.yaml) 为工作区状态结构基线。
- 派发检查决定使用 Sub Agent、阶段 6 准备独立审阅、阶段 7 需要执行或降级已批准的实施编排，或者准备阶段 7 终结代码审阅时，在创建任务包或确定运行方式前完整读取 [references/subagent-delegation.md](references/subagent-delegation.md)。
- 启用或恢复跨对话工作流时，在创建、更新或信任检查点前完整读取 [references/cross-conversation-resumption.md](references/cross-conversation-resumption.md)，并以 [assets/workflow-state.yaml](assets/workflow-state.yaml) 为状态结构基线。
- 阶段 2 开始技术依据核对、阶段 4 核对测试所依赖的项目逻辑、阶段 5 写入或实质修订前，以及阶段 6 因依据或可执行性问题回退时，完整读取 [references/technical-grounding.md](references/technical-grounding.md)。
- 阶段 3 写入前，完整读取 [references/spec-writing.md](references/spec-writing.md)。
- 阶段 4 写入前，完整读取 [references/test-writing.md](references/test-writing.md)。
- 阶段 5 写入前，完整读取 [references/implementation-writing.md](references/implementation-writing.md)。
- 只读取当前阶段的写作指南，不要提前加载后续阶段指南。
- 如果需求提供 Figma 或明确依赖 Figma 行为，在检查设计前读取 [references/figma-grounding.md](references/figma-grounding.md)。
- 使用 Figma 工具前，加载所有强制前置 Figma Skill；存在 `figma-design-to-code` 时，在调用 `get_design_context` 前先加载它。

## 阶段 1：界定方向

工作区模块进入本阶段前必须已成功取得工作区活动槽；未取得时只能登记模块、呈现前序缺口或当前 owner，不得开始方向分析或写入模块产物。

读取当前生效的仓库规则，定位负责该需求的入口或界面，并且只检查足以判断问题和最小可信方向的现有行为。提供 Figma 时，验证准确的 Figma 目标，但将详细设计分析留到后续阶段。

一旦问题、范围、可观察边界和会改变方向的冲突已经能够呈现，就停止继续扫描；API、缓存、事件、生命周期和验证边界留到阶段 2。只呈现问题与期望结果、精简的范围与边界、合理性判断、建议的产品方向、重要替代方案和阻塞项。本阶段不要讨论架构、模块、文件、符号、存储、同步或框架选择。随后请求用户确认方向。

## 阶段 2：核对已批准方向的依据

读取技术依据参考和适用项目规则，并按“职责与 Sub Agent”执行派发检查。像准备立即实施一样，只读走查 `真实入口 → owner → 权威数据源 → 传播 → 可观察消费方`；先确定满足已批准方向所需的最小行为差异、最窄现有修改点和应保持不变的机制，再核对会影响产品行为、落点、保护条件或验证方式的证据。对跨组件状态、网络、缓存、实验、事件或生命周期，确认相关读写/清理方、触发与失败语义，但不得仅因技术风险存在就扩展产品契约。

将发现按“契约晋升门槛”分类，并在工作笔记或 `grounding-pack.md` 中形成最小修改假设：当前路径、必要行为差异、预计修改的既有目标、尚未证明必要的新目标、明确不改的机制，以及会推翻该假设的证据。它不是第四份交付物，也不提前创建 `I-*`。只有在代码漂移、用户改动、可复现性或目标归属会影响方案时，才记录分支、commit 或工作区脏状态证据。

仍存在会改变可观察行为的开放决策时暂停；纯实施约束不进入 Spec，也不成为产品确认项。随后按“适用范围与工作模式”重新判断是否还需要完整三文档工作流；需要时在方向批准的授权下进入阶段 3。

## 阶段 3–5：依次写作与审阅

先应用本 `SKILL.md` 中关于职责、权威、追溯、授权、确认节点和写作的核心规则，再应用仅属于当前阶段的本地写作指南。

### 阶段 3 · Spec

只根据已批准方向、已确认决策和通过契约晋升门槛的行为编写 `spec.md`，不要写入实施约束或机制。逐条删除无法追溯到权威来源、必须保留的既有外部行为或唯一不可避免结果的规则，再说明文档路径和精简的审阅重点并请求批准。

### 阶段 4 · 测试文档

Spec 批准后，读取技术依据与测试写作指南并执行派发检查。`T-*` 只能来自：已批准 `R-*` 的直接外部结果、有证据且必须保留的既有外部回归，或已知会产生不同已批准结果的真实业务入口。失败、恢复、重复、乱序、生命周期和账户边界只有满足该准入条件时才形成行为场景；否则保留为实施约束，不得为了覆盖它们新增产品行为。

每条关键 `R-*` 至少映射到一个 Given-When-Then 行为场景。无法形成已批准的外部可观察结果时，返回阶段 3 或把该项降级为实施约束，不增加内部断言、白盒或独立 UI/手工覆盖。所需验证能力尚不存在时，只记录必须经过的真实集成边界和通过信号，由阶段 5 选择具体目标。重新通读，删除未通过准入的场景并合并结果等价的覆盖，然后说明路径、契约覆盖重点和风险摘要并请求批准。

### 阶段 5 · 实施文档

测试文档批准后，读取技术依据与实施写作指南并执行派发检查。以阶段 2 的最小修改假设为起点；偏离它必须有新证据。把每个关键 `T-*` 沿真实入口、owner、传播边界和可观察消费方走查：已有行为满足的部分给出当前代码依据，需要变化的部分才落到具体 `I-*`。

每个拟新增的生产目标，无论是数据结构、辅助类、owner、状态还是同步机制，都必须通过技术依据参考中的“最小修改与复用优先”负担证明。可测试性、概念整洁、结构对称或未来复用不能单独证明必要性。

由主 Agent 根据已批准的 `spec.md` 与 `test.md` 统一编写 `implementation.md`。步骤以既有目标上的最小新增、修改、移除或迁移为主，并说明满足关键契约所需的职责、真实入口、验证目标和非显然依赖。若某步骤或新目标删除后仍能满足全部已批准需求和回归约束，则删除它；测试场景未通过准入时返回阶段 4。只有存在安全并行包或需要分批控制集成风险时才加入实施编排。阶段 7 结束时的强制只读代码审阅是工作流固定动作，不写入 `implementation.md` 的实施编排、验证要求或 `I-*`。删减后说明路径、Spec/Test 到实施的追溯重点并请求批准。

局部修订继续由同一负责人完成。

## 阶段 6：检查必要性、一致性与可读性

三份文档均已编写并完成各自审阅后，主 Agent 读取委派参考并建立一个独立只读审阅任务。默认派发一个未参与文档主笔或关键技术结论形成的全新、干净上下文 Sub Agent；审阅者只读取三份文档及验证结论所需的最小需求、规则和仓库依据，只返回发现，不编辑文件、不跨越确认节点，也不重新定义产品语义。无法建立独立审阅时报告阻塞，不能以主 Agent 自审替代阶段 6。

主 Agent 回收发现后只做只读核实和来源裁决。只要任何有效发现需要修改 `spec.md`、`test.md` 或 `implementation.md`，就在写入前以 `阶段 6/7 · 独立审阅返修 · 待决定` 呈现发现、证据与严重度、最早应返回阶段、受影响文档和建议修订方向，请用户明确决定；此时保持三份文档不变，阶段 6 不通过。独立审阅结论不能直接改写文档，也不能自动批准阶段 6。用户未授权、拒绝阻塞性修订或选择尚未明确的替代方向时保持阻塞，不得把原文档批准解释为空白返修授权。

取得明确返修授权后，主 Agent 才能修改用户点名的文档，并按最早受影响阶段执行对应写作与依据门槛。独立审阅后的任何文档内容变化都会使当前结论的输入绑定失效；实质修订还必须重新取得所属文档批准，编辑性修订虽不重开文档批准，也同样需要上述阶段 6 返修授权。最终内容确定后，原审阅者仍可调用时执行一次聚焦复审；不可调用时使用新的干净审阅者，对三份最终文档与当前技术依据重新执行完整阶段 6。状态只保留最终规划指纹、当前 reviewer、尝试次数和完整结论，不保存旧任务关系。最终结论未绑定当前规划指纹时，不得通过阶段 6。

启用检查点时，审阅前重新计算 PRD、适用项目规则和关键依据的聚合指纹，并由三份已批准文档与该指纹生成唯一规划指纹；存在相关漂移时先返回最早受影响阶段。

按顺序检查以下六项：

1. **必要性：** 先对照原始 PRD、已确认决策和阶段 2 最小修改假设，检查无来源的 `R-*`、没有外部契约准入的 `T-*` 以及每个新增生产目标；删除后仍能满足全部需求与回归约束的内容属于有效发现。
2. **权威：** 所有规则均通过契约晋升门槛，不存在矛盾、来源越界、职责重复或下游重新定义。
3. **依据：** 每项重要规则、目标、步骤、测试和命令都有支持且保持最新。
4. **可执行性：** 实施顺序、职责归属、测试追溯、验证方式和关键覆盖充分。
5. **精简度：** 交付文档中没有过程历史、重复描述、重复清单、密集表格单元格、通用章节或就绪状态说明。
6. **时效性：** 仓库漂移、阻塞项或后续决策没有使文档失效。

- 编辑性修订不需要重新打开文档批准，但仍须在阶段 6 先取得返修授权。
- 对已授权文档进行实质修订时，先取得阶段 6 返修授权，再更新最早负责该内容的文档并重新打开确认。若同一项技术修正只影响测试与实施且 Spec 不变，可以按该顺序更新两份已授权目标后请求一次联合批准；其他情况在上游重新批准后再更新依赖文档。
- 未经晋升的产品行为进入 Spec 时返回阶段 2；实施约束被提升为 `T-*`，或测试遗漏已批准契约时返回阶段 4；新增生产目标未通过负担证明时返回阶段 5。外部契约、数据 owner、共享状态读写方、生命周期或项目规则存在基础错误时，返回对应最早阶段做聚焦核对和整体修订，不在阶段 6 连续打补丁。
- 如果已批准方向失效，返回阶段 1；如果方向仍然成立但存在未解决的产品决策，在修改 Spec 前返回阶段 2。
- 所有重新打开的审阅均获得批准后，阶段 6 才算通过。

独立审阅无阻塞发现，或其发现已经按上述规则处理且所有重新打开的文档审阅均获批准后，阶段 6 才能通过。报告三份文档路径和独立审阅结论，并询问：`独立必要性、一致性与可读性检查已通过，是否按 implementation.md 开始实施？`

## 阶段 7：仅在确认后实施

获得明确的产品代码实施授权后，先校验三份文档和技术依据指纹仍与授权绑定输入一致。工作区模块还必须用自己的 owner 再执行一次活动槽 `check`；已经失去活动槽或另一个模块正在实施时，不得开始代码写入。

门禁通过后，再按“职责与 Sub Agent”执行派发检查并依据已批准的 `implementation.md` 实施。文档包含实施编排时，运行时任务包必须从批准后的批次和实施包生成；未包含时由主 Agent 按 `I-*` 串行实施。具体映射、降级和停止条件遵循 Sub Agent 参考文件；运行时任务清单不写入跨对话状态。阶段 7 只持续登记本模块实际创建、修改、移动或删除的持久化产品目标。模块完成全部批准验证后，以完整变更清单记录落地目标摘要与输出指纹，并把实施结果记为 `verified`；此时模块仍为 `in_progress`，不得激活下一个模块。

如果实施需要改变已批准的产品规则、目标或文件范围、包边界、依赖顺序、共享集成点、外部契约或风险，立即停止产品代码写入，返回最早受影响的阶段。按阶段 6 的规则完成所有受影响文档的修订、重新审阅和一致性检查后，必须再次取得产品代码实施授权才能继续。

遵循仓库规则，保留用户无关改动。如果实施会改变上游产品规则、新增未经批准的依赖或风险、覆盖未经授权的工作，或者需要外部权限，则停止。除非用户另行要求，否则不要 commit、push、发布、迁移生产数据或执行其他外部变更。

运行已批准的检查，并将结果与三份文档逐一对照。实施结果达到 `verified` 后，立即按 Sub Agent 参考创建一个全新上下文的终结代码审阅任务。审阅者只读检查最终写入清单对应的代码及判断结论所需的最小调用面、三份已批准文档、适用项目规则和验证结果；只返回按严重度排序并带文件或符号证据的缺陷，未发现缺陷时明确报告审阅边界与无发现结论。审阅者不得编辑任何文件，主 Agent 也不得因该审阅自动修改代码或文档、重开既有阶段或请求返修授权。

有效审阅结论无论是“无发现”还是“存在发现”，都直接向用户汇报，并绑定当前规划、实施输出与验证结果生成的终结输入指纹。审阅任务返回有效结论后，先把模块状态标记为 `complete`，再执行工作区门禁的 `complete` 动作释放活动槽；此后下一个模块才可激活。审阅没有形成完整结论时模块保持 `in_progress`；新对话无法继续原 reviewer 时覆盖当前 reviewer 并进行第二次完整审阅，不保存旧任务历史，第二次仍失败时转为 `blocked`。任何场景都不能用主 Agent 自审代替。只返回变更路径、验证结果、代码审阅发现、剩余阻塞项以及与已批准方案的偏差；后续修复仅在用户另行明确请求时作为新的工作处理。

## 输出位置

交付目录优先使用用户明确指定的目录，其次使用项目约定；否则使用 `<git-root>/docs/spec/<feature-slug>/`。普通需求将原始 PRD、`spec.md`、`test.md` 和 `implementation.md` 放在该目录；原始 PRD 按“原始需求归档”移动或创建。跨对话检查点默认使用 `<git-root>/docs/spec/.workflow/<feature-slug>/state.yaml`。`slug` 使用小写连字符格式，文档间统一使用相对链接。不要覆盖未经授权的目标。

需求工作区将唯一原始 PRD 放在 `<spec-root>/<workspace-slug>/` 根部，新模块正式文档放在 `<spec-root>/<workspace-slug>/modules/<module-slug>/`，状态放在 `<spec-root>/.workflow/<workspace-slug>/modules/<module-slug>/state.yaml`。把已经存在且已批准的单模块工作流升级为工作区时，默认保留其原路径并由工作区引用；移动已绑定批准的产物必须先取得明确迁移授权，并按工作区协议重新绑定摘要。
