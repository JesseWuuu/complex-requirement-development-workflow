---
name: complex-requirement-development-workflow
description: "将已有仓库中尚需产品决策的复杂需求，依次转化为 Spec、测试与实施方案，并按确认节点推进实现；支持续接已有工作流，不用于局部修复或已明确的直接实现。"
---

# 复杂需求研发工作流

基于需求、用户决策、设计事实、仓库证据和项目规则，按 `Spec → Test → Implementation` 形成可执行文档，再经单独授权实现代码。规划先确认当前执行路径与必要行为差异，优先复用现有职责和机制。

## 适用范围与工作模式

- 产品方向已确定时直接正式化，只提出实质矛盾或风险。局部修复、纯 UI 直接实现和已经可以直接实施的计划使用更轻量的流程。
- 已有文档或检查点时，从最早未完成或失效阶段恢复，不重复已批准工作。用户限定修改目标时遵守该范围，依赖文档失效须如实说明。
- 阶段 2 后重新判断范围：必要 Diff 为零时给出证据并停止；收敛为单一 owner 内、无待确认产品行为或跨边界风险的局部变更时，停止生成后续文档，单独取得直接实施授权。用户仍要求文档时按已证明的最小范围成文。
- 首次创建交付目录前，概览完整 PRD，按业务语义区分总需求、可独立验收的业务模块和当前处理范围，不依赖需求编号或显式模块标签。多模块归属、目录与严格串行条件按[需求工作区](references/requirement-workspace.md)判断；用户指定其中一项不改变它所属的总需求。

首次启用或用户重新主动调用时，简短说明七阶段、三份交付物、逐项确认和终结只读审阅；正常续接不重复介绍。输入充分时直接开始，按需补充信息。

## 原始需求与输出位置

交付目录优先使用用户指定目录，其次使用项目约定。否则单模块使用 `<git-root>/docs/spec/<feature-slug>/`，多模块使用 `<git-root>/docs/spec/<requirement-slug>/modules/<module-slug>/`；三份文档放在当前模块目录。总需求与模块分别使用业务语义名称，`slug` 使用小写连字符；需求编号仅作可选的原文定位辅助，默认不拼入目录名，也不据此推断执行顺序。已有路径不因新命名规则自动迁移。文档间使用相对链接，写入前说明目标路径。

- 对话中的需求原文保真保存为总需求目录中的 `prd.md`（单模块即其交付目录），不混入分析或后续推断。
- 新建总需求目录时，若本地 PRD 位于该总目录外，先展示总目录结构、PRD 源路径与建议归档目标，再一次性询问是否归档到总需求目录。已有明确移动授权或保留原位的选择时直接复用，不为后续模块重复询问；仅确认业务范围不等于批准移动文件。决定记入检查点的 `inputs.prd.location_decision`。
- 归档待确认时，保留原文件并引用其路径与摘要继续分析，不把未回复当作移动授权或保留原位的批准。移动前核对源、目标及同名冲突，不覆盖不同内容；移动后更新受影响的相对链接、权威路径与绑定，不把路径变化当成新批准。
- 一个大型 PRD 对应多个模块时，各模块引用同一权威路径与摘要，不创建模块级副本或子 PRD。对话原文保存在总需求目录；本地 PRD 按已确认的归档选择处理。

归档或引用不代表批准 PRD 中的方案。已有有效输入直接复用。

## 来源权威与追溯

用户已确认的决策决定产品语义；PRD 描述问题、约束与期望，其建议方案仍需判断合理性；Figma 只证明明确呈现的视觉与交互事实；仓库与项目规则证明当前行为和实施约束。发现实质矛盾时提出，不静默选择。

### 契约晋升门槛

阶段 2 的发现先分类为产品契约、必须保留的现有外部行为、实施约束或开放决策。前两类可以进入 Spec；实施约束留在技术依据或 Implementation；开放决策经用户确认后才晋升。

非上游明示的可观察行为，只有是目标成立时唯一不可避免的结果，或本次必须保留的既有外部行为，才能进入 Spec。存在两种都满足目标而结果不同的选择时，作为开放决策。并发、缓存、生命周期、账户隔离等技术风险不自动产生新产品规则或行为测试。

Spec 负责产品规则；Test 按[场景准入](references/test-writing.md#场景准入)形成覆盖义务；Implementation 承接这些义务并选择最小合理变更。更新最早负责该内容的文档，下游引用，不重复定义。

仅给需要下游追溯的关键规则、测试和主要实施步骤使用 `R-*`、`T-*`、`I-*`；不另建平行 ID 系统。局部修订保留既有 ID，不为符合约定批量重编号。交付文档保留审阅或执行必需的决策及下述修订记录，证据笔记放在交付目录之外；删除重复描述、无关过程历史、废弃方案和空章节，表格仅用于比较。

## 文档修订记录

每次返修 `spec.md`、`test.md` 或 `implementation.md`，都必须在被修改文档内的“修订记录”章节追加本轮记录，覆盖各阶段的编辑性修正与实质返修。同轮涉及多份文档时，各自记录本文件的改动；不能只在对话、检查点或 Git 提交中说明。

每条记录写明修订日期、返修原因、涉及章节或既有 `R-*` / `T-*` / `I-*`，以及具体改动摘要（改了什么、改成什么）。同轮返修合并记录，保留既有记录，不复制完整差异或无关执行日志；首次成文且尚未返修时无需空章节，也不补造历史记录。

正文与修订记录一并完成后，再计算文件摘要、提交审阅或请求批准。修订记录不代表用户批准；授权、失效与复审仍按[文档独立审阅与返修](references/document-review.md)执行。

## 授权与确认节点

用户的明确指令与已有授权优先于默认流程；授权不扩展目标、产品范围或外部操作权限。按当前任务与记录核实授权，不因常规补写或重复读取再次请求许可。

- 默认依次确认方向、Spec、测试文档、实施文档。前一节点批准后可开展下阶段调查并创建下一份文档；这些批准均不等于产品代码授权。
- 实施文档批准后进入阶段 6，并允许主 Agent 在已授权文档范围内作明确的编辑性修正、展示差异。实质返修仍须先取得处置决定，再修改及重批；分类、版本绑定与复审统一按[文档独立审阅与返修](references/document-review.md)执行。
- 阶段 6 通过后，单独确认是否按实施文档开始产品代码实现。阶段 7 的必要实现补充、实质变更及停止边界按[实施规则](references/code-execution.md)执行。
- 会改变可观察行为、范围、外部契约、不可逆数据或安全/隐私义务的产品选择，以及阻止形成可执行结论的证据缺口，需聚焦解决。已验证的命名和落点约定直接应用；判断错误也不改变产品语义的局部假设可以明确范围后保留。
- 仅暂停依赖待决事项或缺失权限的部分。覆盖现有目标、移动用户文件或额外外部操作未获授权时，不执行该动作。

同一任务只有一个明确待确认节点时，“确认”或“通过”可批准该节点；实质返修决定须明确同意建议返回阶段与受影响文档。多决策、多产物或跨对话批准须明确对象。`继续`、进度询问和含糊的“处理一下”不构成批准。

批准记录须来自用户确认并对应其审阅版本。三份文档与技术依据构成规划指纹，阶段 6 结论、代码授权与实施结果绑定该指纹；相关输入实质变化时使受影响批准与授权失效。编辑性修正的处理见文档审阅参考，不通过刷新摘要伪造批准。

## 按需读取与阶段推进

只加载当前阶段需要的参考。同版材料已完整加载且仍在上下文时复用；变化、压缩丢失或截断后补读所缺部分，不能确认完整性时才重读。只需状态和摘要时运行校验脚本，不加载源码或记录读取历史。

| 阶段 | 当前工作与所需参考 | 确认节点 |
| --- | --- | --- |
| 1 · 方向 | 核对相关仓库规则、入口和现有行为；呈现问题、范围、期望结果、建议方向、重要替代方案和阻塞项。足以判断方向即停止扫描，详细机制留到阶段 2。 | 确认方向 |
| 2 · 依据 | 按[技术依据核对](references/technical-grounding.md)沿真实执行路径形成契约分类与最小修改假设，并重新判断工作流适用性。 | 解决阻塞性产品决策 |
| 3 · Spec | 按[Spec 写作](references/spec-writing.md)形成产品规则。 | 审阅 Spec |
| 4 · Test | 复用[技术依据](references/technical-grounding.md)，补齐必要的项目逻辑，按[测试写作](references/test-writing.md)形成行为覆盖义务。 | 审阅测试文档 |
| 5 · Implementation | 按[技术依据](references/technical-grounding.md)的阶段 5 门槛与[实施写作](references/implementation-writing.md)形成具体修改及验证目标；阶段性局部修订由同一负责人完成。 | 审阅实施文档 |
| 6 · 独立文档审阅 | 按[文档独立审阅与返修](references/document-review.md)核实必要性、一致性、依据和可执行性，依影响处置发现。 | 实质返修决定；通过后确认实施 |
| 7 · 代码实施 | 按[实施、验证与终结](references/code-execution.md)实现并记录真实验证结果，随后由全新独立 Sub Agent 只读审阅。验证失败或审阅发现只报告，有效终审结论返回后结束流程。 | 仅在产品代码授权后进入 |

以下参考只在对应条件出现时读取：

- **多模块：** PRD 包含可分别验收的业务模块、用户指定其中一项或提及后续模块时，读取[需求工作区](references/requirement-workspace.md)，先确定总目录与当前模块。满足严格串行条件时才创建 [workspace 模板](assets/workspace.yaml) 对应的调度文件；启用后未取得活动槽不得开始模块工作流，前序未完成不得激活后续模块。
- **检查点：** 首次阶段产出时、恢复任务或上下文丢失时，读取[跨对话续接](references/cross-conversation-resumption.md)，按其中单模块或多模块路径使用 [state 模板](assets/workflow-state.yaml)。用 `check_resume_state.py --context` 获取当前阶段输入，需摘要时加 `--fingerprints`。
- **委派：** 准备派发调查、实施或强制审阅任务时，读取[Sub Agent 派发与回收](references/subagent-delegation.md)。
- **Figma：** 需求提供或依赖设计事实时，在检查设计前读取[Figma 依据](references/figma-grounding.md)，并遵守所用 Figma 工具的强制前置 skill。

## 职责与输出

主 Agent 负责产品语义、用户沟通、三份文档主笔、确认节点、共享边界及整合。阶段 2、4、5、7 只将有实质并行收益且边界独立的调查或实施交给 Sub Agent；强串行、重叠写入和局部小修自行完成。阶段 6 与阶段 7 的独立只读审阅是固定门槛，不能以主 Agent 自审替代；独立性、配额与失败恢复按委派参考执行。

正式阶段产出、确认、阻塞和完成报告以 `<需求代号> · 阶段 N/7 · <名称> · <状态>` 开头，呈现结论、必要决策和下一步；常规更新不重复状态栏。单模块代号沿用交付目录末级名称，多模块使用 `<总需求代号>/<模块代号>`，不另造缩写。

首次阶段产出保存检查点，说明交付目录并给出 `继续 <需求代号>`（多模块附模块代号）。无法唯一定位时只询问项目、模块或路径，不新建同名流程。运行时直接提供可靠上下文窗口统计时可附使用比例；否则省略，不扫描日志或估算。
